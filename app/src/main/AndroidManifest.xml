<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="co.com.script.ciclos">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" />
        <activity
            android:name="co.com.script.ciclos.LoginActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="co.com.script.ciclos.HomeActivity"
            android:label="@string/title_activity_home"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.ProfileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.ReporteActivity"
            android:label="@string/title_activity_reporte"
            android:theme="@style/ReportTheme" />
        <activity
            android:name="co.com.script.ciclos.ListReporteActivity"
            android:label="@string/title_activity_list_reporte"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.GalleryActivity"
            android:label="@string/title_activity_gallery"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.RutaActivity"
            android:label="@string/title_activity_ruta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.PlanillaActivity"
            android:label="@string/title_activity_planilla"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.ChatActivity"
            android:configChanges="keyboardHidden"
            android:label="@string/title_activity_chat"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="co.com.script.ciclos.ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.CalendarActivity"
            android:label="@string/title_activity_calendar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.AboutActivity"
            android:label="@string/title_activity_about"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.PiscinasActivity"
            android:label="@string/title_activity_piscinas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.RutaPActivity"
            android:label="@string/title_activity_ruta_p"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="co.com.script.ciclos.MapRutaActivity"
            android:label="@string/title_activity_map_ruta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.NotificationActivity"
            android:label="@string/title_activity_notification"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.AlarmActivity"
            android:label="@string/title_activity_alarm"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.com.script.ciclos.MapCasaActivity"
            android:label="@string/title_activity_map_casa"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.liuguangqiang.ipicker.IPickerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
    </application>

</manifest>